# dsh-background bundle patch: inserts the plugin row into the web profile
# roster. The node half (exports ".") registers the `background` settings
# namespace, and the `dsh.client` declaration in package.json makes the
# browser half (exports "./client") load in the web GUI.
- insert:
    - id: ui-background
      name: dsh-background
